<h2>New submission</h2>

<%= render "form.html", changeset: @changeset,
                        durations: @durations,
                        presenter: @presenter,
                        conn: @conn,
                        action: submission_path(@conn, :create) %>