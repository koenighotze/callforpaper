<h2>Edit submission</h2>

<%= render "form.html", changeset: @changeset,
                        conn: @conn,
                        callforpapers: @callforpapers,
                        talks: @talks,
                        action: submission_path(@conn, :update, @submission) %>
