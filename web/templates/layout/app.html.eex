<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Basic app for handling talk-submissions">
    <meta name="author" content="David Schmitz">

    <title>Talk Submission Administration</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/font-awesome.min.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <%= if @current_user do %>
              <li><p><%= @current_user.name %> (<%= @current_user.role %>) </p></li>
              <li>
                <%= link to: submission_path(@conn, :index), title: "Submissions" do
                  fa_icon("book")
                end
                %>
              </li>
              <li>
                <%= if Callforpapers.User.is_organizer?(@current_user) do %>
                  <%= link fa_icon("user"), to: user_path(@conn, :index), title: "Presenters" %>
                <% else %>
                  <%= link fa_icon("user"), to: user_path(@conn, :show, @current_user), title: "Presenters" %>
                <% end %>
              </li>
              <li>
                <%= link  fa_icon("power-off"), to: session_path(@conn, :delete, @current_user), method: "delete", title: "Logout" %>
              </li>
              <li><%= link fa_icon("home"), to: page_path(@conn, :index), title: "Home" %></a></li>
            <% else %>
              <li><%= link to: user_path(@conn, :new), title: "Register" do
                fa_icon("user-plus", text: "Register")
              end %></li>
              <li><%= link to: session_path(@conn, :new), title: "Login" do
                fa_icon("user-circle", text: "Login")
              end
              %></li>
            <% end %>

          </ul>
        </nav>
        <span class="logo"></span>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
