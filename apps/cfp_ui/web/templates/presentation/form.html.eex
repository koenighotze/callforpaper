<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :held_at, class: "control-label" %>
    <%= datetime_select f, :held_at, class: "form-control" %>
    <%= error_tag f, :held_at %>
  </div>

  <div class="form-group">
    <%= label f, :held_where, class: "control-label" %>
    <%= text_input f, :held_where, class: "form-control" %>
    <%= error_tag f, :held_where %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
